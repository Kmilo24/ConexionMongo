<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <%if(title) {%>
    <h1><%= title %></h1>
    <%}%>

    <% if(users) {%>
       <%users.forEach( (user) => { %>
          <%- include('user-partial', {user})%>
        <%});%>
    <%}%>
  </body>
</html>
